---
title: Listar Envelopes
description: Retorna uma lista paginada de envelopes com filtros opcionais de busca, data e status.
openapi: 'GET /api/envelopes'
---

<RequestExample>
<CodeGroup>

```bash cURL
curl -G "https://api.formsign.com.br/api/envelopes" \
  -H "Authorization: Bearer <TOKEN>" \
  --data-urlencode "page=1" --data-urlencode "pageSize=10"
```

```javascript JavaScript
await fetch('https://api.formsign.com.br/api/envelopes?page=1&pageSize=10',{
  headers:{'Authorization':'Bearer <TOKEN>'}
});
```

```python Python
import requests
r=requests.get('https://api.formsign.com.br/api/envelopes',params={'page':1,'pageSize':10},headers={'Authorization':'Bearer <TOKEN>'})
print(r.json())
```

```go Go
package main
import ("net/http"; "fmt")
func main(){
  req,_:=http.NewRequest("GET","https://api.formsign.com.br/api/envelopes?page=1&pageSize=10",nil)
  req.Header.Set("Authorization","Bearer <TOKEN>")
  res,_:=http.DefaultClient.Do(req)
  fmt.Println(res.Status)
}
```

```java Java
// GET com query params page/pageSize e Authorization: Bearer <TOKEN>
```

</CodeGroup>
</RequestExample>

### Parâmetros de Query

<ParamField query="page" type="integer" default="1">Número da página.</ParamField>
<ParamField query="pageSize" type="integer" default="10">Tamanho da página (máx 100).</ParamField>
<ParamField query="searchText" type="string">Texto para busca.</ParamField>
<ParamField query="lastUpdateStartDate" type="string">Filtro por data de atualização (início).</ParamField>
<ParamField query="lastUpdateEndDate" type="string">Filtro por data de atualização (fim).</ParamField>
<ParamField query="status" type="string">Filtro de status.</ParamField>

### Respostas

<ResponseExample>
```json 200
{
  "page": 1,
  "pageSize": 10,
  "totalPages": 2,
  "totalRecords": 12,
  "envelopes": [ { "envelopeId": "env_123", "status": "pending" } ]
}
```

```json 401
{ "error": "Unauthorized" }
```
</ResponseExample>
