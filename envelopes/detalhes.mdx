---
title: Obter Envelope
description: Retorna os dados completos do envelope, incluindo status, datas e destinatários.
openapi: 'GET /api/envelopes/{envelopeId}'
---

<RequestExample>
<CodeGroup>

```bash cURL
curl -X GET "https://api.formsign.com.br/api/envelopes/env_123" \
  -H "Authorization: Bearer <TOKEN>"
```

```javascript JavaScript
await fetch('https://api.formsign.com.br/api/envelopes/env_123',{
  headers:{'Authorization':'Bearer <TOKEN>'}
});
```

```python Python
import requests
r=requests.get('https://api.formsign.com.br/api/envelopes/env_123',headers={'Authorization':'Bearer <TOKEN>'})
print(r.json())
```

```go Go
package main
import ("net/http"; "fmt")
func main(){
  req,_:=http.NewRequest("GET","https://api.formsign.com.br/api/envelopes/env_123",nil)
  req.Header.Set("Authorization","Bearer <TOKEN>")
  res,_:=http.DefaultClient.Do(req)
  fmt.Println(res.Status)
}
```

```java Java
// Use HttpClient para GET com Authorization: Bearer <TOKEN>
```

</CodeGroup>
</RequestExample>

### Parâmetros

<ParamField path="envelopeId" type="string" required>
ID do envelope a ser consultado.
</ParamField>

### Respostas

<ResponseExample>
```json 200
{
  "envelopeId": "env_123",
  "status": "pending",
  "subject": "Assine o contrato",
  "recipients": [{"email":"cliente@exemplo.com","name":"Cliente","status":"pending"}]
}
```

```json 404
{ "error": "Envelope not found" }
```

```json 401
{ "error": "Unauthorized" }
```
</ResponseExample>

### Campos de resposta (principais)

<ResponseField name="envelopeId" type="string" required>
Identificador do envelope.
</ResponseField>
<ResponseField name="status" type="string" required>
Estado atual do envelope (ex.: pending, completed, void).
</ResponseField>
<ResponseField name="recipients" type="array">
Lista de destinatários e respectivos status.
</ResponseField>
