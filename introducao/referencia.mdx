---
title: Comece por aqui
description: API FormSign Base64 - Envie documentos PDF diretamente para assinatura eletrônica.
icon: rocket
---

Bem-vindo à API FormSign Base64! Esta documentação foca especificamente nas APIs que permitem enviar documentos PDF diretamente no formato base64, ideais para integrações server-to-server que não querem gerenciar uploads de arquivos.

## O que é diferente nas APIs Base64?

🚀 **Upload direto**: Envie o PDF codificado em base64 diretamente na requisição  
📦 **Sem S3**: Não precisa fazer upload prévio para S3  
⚡ **Mais simples**: Um único endpoint para cada operação  
🛡️ **Validação automática**: PDF é validado automaticamente (tamanho, formato)  
🔧 **Server-to-server**: Ideal para automações e integrações  

## Como funciona em 2 passos

### 1) Enviar envelope com documento base64
Use o endpoint `POST /api/envelopes/send/base64` para criar e enviar um envelope com PDF em base64.

### 2) Ou criar template com documento base64  
Use o endpoint `POST /api/templates/base64` para criar templates reutilizáveis com PDF em base64.

## Exemplo rápido: Enviar envelope

```bash
# Converter PDF para base64 (Linux/macOS)
PDF_BASE64=$(base64 -w 0 contrato.pdf)

# Enviar envelope
curl -X POST "https://api.formsign.com.br/api/envelopes/send/base64" \
  -H "Authorization: Bearer <SEU_TOKEN>" \
  -H "Content-Type: application/json" \
  -d "{
    \"subject\": \"Contrato para Assinatura\",
    \"message\": \"Por favor, revise e assine o documento.\",
    \"submissionType\": \"document\",
    \"base64Document\": \"$PDF_BASE64\",
    \"documentName\": \"contrato.pdf\",
    \"fields\": [
      {
        \"type\": \"signature\",
        \"identifier\": \"assinatura_cliente\",
        \"required\": true,
        \"x\": 70,
        \"y\": 90,
        \"w\": 20,
        \"h\": 8,
        \"pageNumber\": 1
      }
    ],
    \"recipients\": [
      {
        \"email\": \"cliente@example.com\",
        \"name\": \"João Silva\",
        \"signatureType\": \"draw\",
        \"role\": \"signer\"
      }
    ]
  }"
```

## Autenticação

Para usar as APIs, você precisa de um token Bearer. Obtenha via:

```bash
curl -X POST "https://api.formsign.com.br/api/token" \
  -H "Authorization: Basic $(echo -n 'CLIENT_ID:CLIENT_SECRET' | base64)"
```

## Validações importantes

- ✅ **PDF válido**: Documento deve ter magic bytes `%PDF`
- ✅ **Tamanho máximo**: 10MB por documento  
- ✅ **Extensão**: Nome do arquivo deve terminar com `.pdf`
- ✅ **Base64**: Formato correto, sem prefixos data URL

## Próximos passos

- 🎯 **Templates**: Crie modelos reutilizáveis → `Templates Base64`
- 📧 **Envelopes**: Envie documentos para assinatura → `Envelopes Base64`  
- 🔐 **Autenticação**: Configure tokens → `Autenticação`
- 💡 **Exemplos**: Veja código completo → `Exemplos`